# Changelog

## v2.3.0-dev (2026-05-31)

- 统一版本号口径：Python 包版本更新为 `2.3.0.dev0`，文档展示版本统一为 `v2.3.0-dev`。
- 抽出产出质量评价共享契约：`server.py` 与 `demo/api/review.py` 共用 `review_contract.py` 中的评价维度、prompt 版本、fallback 和响应补全逻辑。
- 评价响应新增 `prompt_version` 字段，前端导出的 Markdown 报告可追踪评价契约版本。
- 文档同步：SKILL.md 补充协议设置（时间偏好/产出模式/协议强度）、三阶段计时器、碎纸机模式、停滞自动介入、附件上下文、语音输入、产出质量评价、会话持久化等代码已实现但协议文档未记录的功能。
- 修复步骤数约束：`server.py` AI 拆解验证从 3-5 步修正为 3-6 步，`demo/api/_common.py` prompt 同步修正，与 SKILL.md 对齐。
- README 重写：补充精确技术栈版本号、技术亮点（强调 why 而非 what）、功能状态表、项目结构。
- SKILL.md 版本号升至 2.3.0-dev。

## v2.2.0-dev (2026-05-30)

- 新增产出质量评价系统：Web 评价页与 `scripts/review.py` CLI 共用 5 个评价维度，支持生成 Markdown 报告并保存历史。
- 新增附件上下文能力：支持 PDF、DOCX、XLSX、PPTX 解析，并将附件摘要带入任务拆解和步骤帮助。
- 新增协议设置：支持协议强度（温和 / 标准 / 严厉）、时间偏好（紧凑 / 标准 / 宽松）和产出模式（草稿 / 可交付 / 作品集）。
- 改进改进循环：Done 页“只改一处”回到 Roadmap，高亮 AI 指定步骤，并在页面内展示轮次警告，不再依赖原生 `confirm()`。
- 新增摘要归档接口：`POST /api/summarize` 将每步原始产出压缩成破冰战报摘要。
- 新增 Vercel serverless demo 接口：`demo/api/` 提供轻量 `chat`、`chat/stream`、`summarize`、`attachments/parse` fallback。
- 改进 PWA 与缓存配置：补充 manifest、图标和 Vite PWA 构建链路，修复缓存图标残留问题。
- 文档更新：README 改为完整项目说明，补充技术栈、启动方式、API、目录结构和当前功能状态。

## v2.1.0 (2026-05-14)

- 新增改进循环：Done 页“继续改进”由 AI 决定改什么，用户只管确认。
- 改进回到 Roadmap，高亮要改的步骤，轮次可视化。
- 软性轮次警告：第 3 轮温和提醒，第 4 轮强警告，不硬截断。
- 修复“继续改进”按钮 fall through 到 Contract 页的 Bug。

## v2.0.0 (2026-05-13)

- 核心流程重构：从“三步启动”升级为“拆解 → 执行 → 成型”。
- 协议不再只解决启动问题，而是引导用户做出完整雏形。
- 任务拆解为 3-6 步，每步有明确产出，逐步执行直到成型。
- Demo 重构：AI 驱动的全屏对话体验，内嵌路线图、步骤、倒计时卡片。
- 移除“30 分质量目标”，改为“做出可以改的初稿”。
- 移除独立场景选择 / 方向探索页面，由 AI 对话自然处理。

## v1.0.0 (2026-05-08)

- 初始版本。
- 核心三步启动流程：启动契约、荒谬拆解、思考预算。
- 羞耻感重构话术库。
- 方向探索快速收敛。
- 社交模板库和求职原子步骤。
- 倒计时脚本 `scripts/timer.py`。
- 交互式网页演示 `demo/index.html`。
