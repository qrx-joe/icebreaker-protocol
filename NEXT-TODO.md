# 破冰协议 · Next To-Do

> 当前版本: v2.0.0 | 最后更新: 2026-05-31

> ⚠️ **执行顺序提醒**：第 9 节「AI 评价规范」和第 10 节「评价页 UI 升级」依赖第 0 节先解决——
> AI 评价当前根本没成功跑过，规范和样式都需要真实评价数据才能验证。
> 先修 #0 让真 AI 评价能返回 → 再按 #9 固化规范 → 最后做 #10 视觉升级。

---

## 🔴 高优先级（阻塞/影响核心体验）

### 0. AI 评价始终显示"暂不可用"
**状态**: 后端路由已实现，但前端仍显示 fallback 文案
**根因分析**:
- `_ai_review()` 调用 `client.chat.completions.create()` 时可能抛出异常（API Key 无效、网络超时、模型返回格式错误）
- 异常后落入 `_fallback_review()`，返回 `"mode": "local"` 和 `"summary": "AI评价暂时不可用..."`
- 前端 `review.js:219` 检测到 `data.mode === 'local'` 时弹出 toast，但未明确告知用户是 API Key 问题还是网络问题

**待排查**:
- [ ] 确认 `.env` 中的 `DEEPSEEK_API_KEY` 是否有效（curl 直接测试 API）
- [ ] 确认 `_ai_review()` 异常时是否吞掉了有用的错误信息（当前 `except Exception: return {}`）
- [ ] 前端未区分"API Key 缺失"和"API 调用失败"，用户无法自助排查

**修复方案**:
- [ ] `_ai_review()` 异常时返回具体错误类型（`api_key_missing` / `timeout` / `invalid_response`）
- [ ] 前端根据错误类型显示不同的引导文案（如"请检查 .env 中的 API Key"）
- [ ] 在设置面板中增加 API Key 配置入口和连通性测试按钮

---

### 1. 前端样式渲染稳定性
**状态**: 部分场景下仍无法渲染（白底无样式）
**根因**: 
- Vite 构建的 `dist/` 目录被 FastAPI 静态文件服务挂载后，浏览器仍可能加载缓存的旧版本
- PWA Service Worker 缓存策略导致旧版本 `index.html`（引用 `/src/main.js`）被优先返回

**解决方案**:
- [ ] 在 `vite.config.js` 中配置 `workbox` 的 `skipWaiting` 和 `clientsClaim`，确保新版本立即生效
- [ ] 为 `index.html` 添加缓存控制头 `Cache-Control: no-cache, no-store, must-revalidate`
- [ ] 或在 FastAPI 中为 `index.html` 单独设置无缓存响应头（而非通过 `StaticFiles`）
- [ ] 考虑在 `index.html` 中添加版本戳查询参数（如 `?v=2.0.0`）强制刷新

**验证方式**: 关闭浏览器后重新打开，首次访问即显示深色主题样式

---

### 2. `demo/uv.lock` 是否应加入 `.gitignore`
**状态**: 已提交到仓库
**问题**: `uv.lock` 是 uv 包管理器的锁定文件，通常用于 Python 依赖锁定。但 `demo/` 目录是前端项目（npm/vite），此文件可能是误生成。

**行动**:
- [ ] 确认 `demo/uv.lock` 的来源和必要性
- [ ] 如不需要，从仓库移除并加入 `.gitignore`

---

## 🟡 中优先级（功能完善）

### 3. 统一开发/生产启动方式文档
**状态**: README 未更新本次改动
**问题**: 当前项目支持两种启动方式，但文档未明确说明：
- **开发模式**: `cd demo && npm run dev`（Vite port 3000）+ `uv run python main.py`（FastAPI port 8000）
- **生产模式**: `cd demo && npm run build` + `uv run python main.py`（FastAPI 直接服务 dist/）

**行动**:
- [ ] 更新 README.md，添加清晰的「快速启动」章节
- [ ] 说明两种模式的适用场景和端口映射
- [ ] 添加环境变量配置说明（`.env` 文件）

---

### 4. `/api/review` 端点缺少响应模型
**状态**: 已实现但无 Pydantic 模型验证
**问题**: `server.py` 中的 `review` 端点返回 `dict`，FastAPI 无法生成正确的 OpenAPI 文档

**行动**:
- [ ] 为 `ReviewResponse` 创建 Pydantic 模型（含 `dimensions` 列表）
- [ ] 统一 `normalizeReview` 逻辑（前端 `review.js` 与后端 `_fallback_review` 的评分算法应保持一致）

---

### 5. PWA 资源 404
**状态**: `icon.svg` 和 `manifest.json` 在某些场景下 404
**根因**: 构建后的 `dist/` 目录包含这些文件，但 `StaticFiles(html=True)` 的 SPA fallback 可能干扰

**行动**:
- [ ] 验证 `dist/icon.svg` 和 `dist/manifest.json` 是否能直接访问
- [ ] 如不能，检查 `StaticFiles` 的 `check_dir` 行为或改用显式路由

---

## 🟢 低优先级（优化/重构）

### 6. 代码质量
- [ ] `server.py` 中的 `REVIEW_PROMPT` 和 `REVIEW_DIMENSIONS` 可提取到独立模块（如 `server/review.py`）
- [ ] `demo/api/review.py`（Vercel serverless 版本）与 `server.py` 中的 review 逻辑重复，考虑复用
- [ ] `server.py` 已超过 800 行，建议按功能拆分为多个模块

### 7. 测试覆盖
- [ ] 为 `/api/review` 添加单元测试（有 API Key 时返回 AI 评价，无 Key 时返回 fallback）
- [ ] 为静态文件服务添加测试（确认 `dist/` 资源可访问）

### 8. 部署优化
- [ ] Vercel 部署时，`demo/api/` 下的 serverless 函数是否仍能正常工作？
- [ ] `demo/api/_common.py` 中的 `load_dotenv()` 在 Vercel 环境（无 `.env` 文件）下是否安全？

---

### 9. AI 评价规范设计
**状态**: 需设计 · **前置依赖**: #0（AI 评价能跑通）
**背景**: 当前 AI 评价只有 5 个维度、1-5 分、零权重，prompt 是"一次性"的——没有校准、没有自检、没有版本管理。一旦换模型或换 prompt，评价结果可能漂移但无人察觉。

**9.1 评分维度与权重体系**
- [ ] 定义 5 维度的**权重矩阵**（当前是均权 1:1:1:1:1，实际"完成度"应比"受众匹配"更重要）
- [ ] 为每个维度制定**评分锚点**（1 分 = 什么表现 / 3 分 = 什么表现 / 5 分 = 什么表现），写入 `REVIEW_PROMPT`
- [ ] 增加**总体等级**映射：总分 → S/A/B/C/D 五档，替代纯数字，降低用户对 ±1 分的焦虑

**9.2 Prompt 契约与版本管理**
- [ ] `REVIEW_PROMPT` 提取到独立文件 `server/prompts/review_v1.txt`，支持 git diff 追踪
- [ ] 在评价响应中增加 `prompt_version` 字段，前端/导出报告可见
- [ ] 定义 `ReviewResponse` Pydantic 模型（补 #4），作为前后端契约的单一事实来源

**9.3 评价质量自检（Meta-Eval）**
- [ ] 评价结果中增加 `confidence` 字段：AI 自评本次评价的置信度（high/medium/low）
- [ ] 增加一致性校验：若某维度 `score=5` 但 `comment` 含"缺失/不足/模糊"等否定词，标记为 `flagged: true`
- [ ] 评价结果持久化到 `localStorage` 后，支持同一任务多次评价的**趋势对比**（进步/退步/持平）

**9.4 Fallback 与 AI 评价的边界**
- [ ] 明确定义何时走 fallback：API Key 缺失 / 超时 / 返回格式非法 / 模型降级
- [ ] Fallback 评分逻辑不能伪装成 AI 评价——当前 `mode: "local"` 但 UI 不区分，需在评价卡片上标注数据来源
- [ ] Fallback 的维度 comment 应更保守（不假装分析，只说"本地规则判断"）

**9.5 可扩展性预留**
- [ ] 维度列表支持**动态配置**：从 `REVIEW_DIMENSIONS` 常量 → 可由协议模板自定义（不同协议强度 → 不同评价维度）
- [ ] 预留"自定义维度"入口：用户可在设置中添加/禁用维度（前端 UI 后做，先留数据结构）

---

### 10. 评价页 UI 升级
**状态**: 需设计 · **前置依赖**: #0 + #9
**背景**: 当前评价页是纯文本列表，信息层级模糊，视觉上像调试输出不像产品功能。

**10.1 信息层级重构**
- [ ] **总评卡片**：总分用大字 + 等级徽章（S/A/B/C/D），替代当前的 `15/25` 纯数字
- [ ] **最优先修改**独立浮动卡片，放在最上方，带醒目边框/背景色（当前与"主要问题"同级，不突出）
- [ ] **维度评分**改为横向进度条 + 分数，替代当前的 `3/5` 纯文本（进度条颜色：1-2 红、3 黄、4-5 绿）

**10.2 视觉风格**
- [ ] 评价结果整体采用**深色半透明卡片**风格，与破冰协议的暗色主题统一
- [ ] 问题列表用红色小圆点标记，优点用绿色对勾标记（替代当前的纯文本 `<ul>`）
- [ ] 维度 comment 文字调小一号、增加行间距，与维度名称形成主次对比

**10.3 交互增强**
- [ ] 评价加载动画：当前的 spinner-ring 过于朴素，改为**逐维度打字机效果**（AI 返回后维度逐个淡入，每个维度间隔 200ms）
- [ ] "导出报告"按钮增加预览：点击后先弹出 Markdown 预览弹窗，确认后再下载
- [ ] 评价历史：在评价页底部增加"历史评价"折叠面板，显示同一任务的历次评价趋势

**10.4 状态与错误体验**
- [ ] AI 评价失败时：显示**具体原因卡片**（API Key 缺失 / 网络超时 / 返回格式错误），而非一个 toast
- [ ] Fallback 评价结果顶部增加**信息条**："本次评价由本地规则生成，配置 AI Key 后可获得更精准的分析"
- [ ] 评价进行中增加**取消按钮**，允许用户中途退出回到结案页

---

### 11. 时间管理/学习管理方法论集成
**状态**: 需设计与开发 · **前置依赖**: #0（需要 AI 拆解能力稳定）
**核心定位**: 破冰协议治"开不了头"，方法论治"执行过程"。方法论不是替代品，是**加速器**——协议负责拆解和启动，方法论负责执行节奏。
**设计原则**:
- 方法论**内嵌到步骤级**，不是独立模块——用户在执行某一步时自然触达，不需额外学习
- 方法论推荐由 AI 拆解时**自动匹配**，用户可关闭（设置面板有全局开关）
- 评价时记录"使用了哪个方法论"到 `methodologies_used` 字段，与 #9 评价规范联动

**11.1 场景一：朋友圈 → Timeboxing 时间盒**
**匹配理由**: 朋友圈是 5-8 分钟超短任务，番茄钟 25 分钟会撑爆。时间盒的核心是"给每个步骤一个硬截止时间，到了就停"，与破冰协议的"乱写期→修整期"三阶段天然互补。

- [ ] **步骤时间盒 UI**：路线图每个步骤右侧增加一个可编辑的倒计时徽章（默认 AI 推荐时长，如"定基调 2min"）
- [ ] **倒计时运行时**：步骤执行时显示环形进度条（替代/增强现有计时器），时间到时**非侵入式提示**（进度条变红 + 轻微震动，不弹窗打断心流）
- [ ] **时间盒规则嵌入 prompt**：拆解 prompt 增加"为每个步骤估算时间盒时长（1-10 分钟），标注在步骤 title 后"
- [ ] **超时宽容**：时间盒不是硬锁——超时后允许继续，但进度条变红 + 文案从"乱写期"切换为"超时了，先交出来"
- [ ] **演示脚本补充**：在 `docs/demo-scenarios.md` 场景一的脚本中，第 1 步展示时间盒倒计时，口头说明"时间盒比番茄钟更适合超短任务"

**11.2 场景二：述职 PPT → Pomodoro 番茄钟 + Eisenhower 矩阵**
**匹配理由**: 述职是 30 分钟-2 小时中等任务，番茄钟 25 分钟恰好切 1-2 个步骤。Eisenhower 矩阵解决"列成果时什么都想塞进去"的筛选焦虑。

- [ ] **番茄钟模式**：当 AI 判断总任务时长 > 20 分钟时，自动建议开启番茄钟模式（路线图顶部出现🍅开关）
- [ ] **番茄钟计时器**：25 分钟工作 + 5 分钟休息的循环，与步骤进度联动（一个番茄钟可能覆盖 1-2 个步骤）
- [ ] **Eisenhower 筛选步骤**：当步骤含"列 N 个/写 N 条"类型的指令时，步骤卡片增加一个"矩阵筛选"按钮——点击弹出 2×2 矩阵，用户把列出的条目拖入四个象限，只保留"重要且紧急"的进入下一步
- [ ] **矩阵 UI 组件**：轻量级四象限拖拽面板（移动端用点击归类代替拖拽），完成后自动精简步骤产出
- [ ] **拆解 prompt 增强**：识别"列成果/列要点"类步骤时，增加子提示"建议用户使用 Eisenhower 矩阵筛选，只保留最重要的 3 条"
- [ ] **演示脚本补充**：在 `docs/demo-scenarios.md` 场景二中，第 2 步"列 3 个成果"后展示矩阵筛选，口头说明"不是所有成果都值得放进 PPT"

**11.3 场景三：README → MVP 思维 + MIT 最重要一件事**
**匹配理由**: README 是完美主义重灾区——"总想写完美导致一直拖着不写"。MVP 思维强制定义"最小可用版"是什么，MIT 强制只做一件事，是治"写不出"的根因而非时间管理。

- [ ] **MVP 定义步骤**：当 AI 识别任务类型为"创作/文档/设计"时，在路线图第 1 步前插入一个"MVP 声明"步骤——要求用户写出"最小可用版 = 什么"，作为后续所有步骤的完成标准
- [ ] **MVP 声明 UI**：一个醒目的置顶卡片（路线图上方），内容是用户写的 MVP 定义，始终可见
- [ ] **MIT 标记**：路线图中一个步骤带金色星标⭐（MIT = Most Important Task），AI 自动标记最关键的一步；其余步骤标记为"可选"（灰色文字，完成加分但不影响通过）
- [ ] **MVP 守门规则**：改进循环时，AI 评价标准改为"是否满足 MVP 声明"而非"是否完美"，与 #9 评价规范的权重联动
- [ ] **拆解 prompt 增强**：创作类任务增加子提示"第一步定义 MVP（最小可用版包含什么），标记一个 MIT 步骤"
- [ ] **演示脚本补充**：在 `docs/demo-scenarios.md` 场景三中，展示 MVP 声明卡片和 MIT 标记，口头说明"完美主义者的解药不是更多时间，而是更小的完成标准"

**11.4 场景四：拒绝邮件 → 2-Minute Rule + SBI 反馈模型**
**匹配理由**: 拒绝邮件是情绪敏感的短任务——不敢写、怕措辞错。2 分钟法则解决"启动焦虑"（2 分钟能做完的事不该拖延），SBI（Situation-Behavior-Impact）提供结构化写作框架，避免"不知道怎么措辞"的瘫痪。

- [ ] **2 分钟法则标记**：当 AI 判断任务总时长 < 5 分钟时，路线图顶部显示"⚡2 分钟任务"徽章，提示"这类任务不该拖延，2 分钟能搞定"
- [ ] **SBI 写作框架**：当步骤涉及"写邮件/回复/反馈"时，步骤卡片增加"SBI 框架"折叠面板：
  - **S**ituation：描述具体情境（"上周的会议中..."）
  - **B**ehavior：描述客观行为（"你提出了..."）
  - **I**mpact：描述影响（"这让团队..."）
- [ ] **SBI 模板填充**：点击 SBI 面板后，自动把步骤的 expected_output 映射成 S/I/B 三个输入框，降低写作门槛
- [ ] **情绪缓冲**：涉及"拒绝/批评/负面"关键词的任务，协议强度自动提升（增加更多检查点），与现有 `protocol_strength` 联动
- [ ] **拆解 prompt 增强**：识别"拒绝/批评/反馈"类任务时，增加子提示"建议使用 SBI 框架（情境-行为-影响）组织措辞，避免情绪化表达"
- [ ] **演示脚本补充**：在 `docs/demo-scenarios.md` 场景四中，展示 SBI 框架折叠面板和 2 分钟徽章，口头说明"2 分钟能写完的邮件不该拖 3 天"

**11.5 方法论底层架构**
- [ ] 创建 `demo/src/methodologies/` 目录，每个方法论一个 JS 模块（`timeboxing.js` / `pomodoro.js` / `eisenhower.js` / `mvp.js` / `mit.js` / `two-minute.js` / `sbi.js`）
- [ ] 统一方法论接口：`{ id, name, icon, match(taskMeta): boolean, activate(stepEl, stepData): void, deactivate(): void }`
- [ ] 设置面板增加"方法论"标签页：全局开关 + 每个方法论的独立开关
- [ ] 拆解响应增加 `methodologies_suggested` 字段：AI 返回推荐的方法论列表，前端据此激活
- [ ] 评价响应增加 `methodologies_used` 字段：记录本次执行中实际使用了哪些方法论

**11.6 学习管理模块（需新增学习场景）**
**当前瓶颈**: 现有 4 个场景全是"写作/产出"类，硬塞费曼/Anki/SQ3R 会错位。必须先增加学习类场景。

**推荐引入的学习方法论**:
- **费曼学习法**：适合"准备技术分享/写教程"场景——步骤为"选概念 → 用大白话解释 → 找到卡点 → 回去补 → 简化"
- **SQ3R**（Survey-Question-Read-Recite-Review）：适合"读一本书/看一篇论文做笔记"场景
- **间隔重复/Anki 卡片生成**：适合"考前复习/记知识点"场景——产出直接导出 Anki `.apkg` 文件

- [ ] 在 `docs/demo-scenarios.md` 新增场景五："准备一次 10 分钟的技术分享"（匹配费曼学习法）
- [ ] 在 `docs/demo-scenarios.md` 新增场景六："读完一本书做可复用的笔记"（匹配 SQ3R）
- [ ] 实现 `feynman.js` 方法论模块：步骤自动映射费曼 4 步（解释→卡点→补→简化）
- [ ] 实现 `sq3r.js` 方法论模块：5 步拆解 + 每步有专属引导
- [ ] 实现 `spaced-repetition.js` 方法论模块：产出阶段自动生成 Anki 卡片 JSON，支持 `.apkg` 导出
- [ ] 评价维度扩展：学习类场景增加"可复述性"和"卡点识别度"维度（与 #9.5 动态维度联动）

---

## 📋 已完成的改动（本次会话）

| 改动 | 文件 | 说明 |
|---|---|---|
| ✅ 添加 `/api/review` 路由 | `server.py` | FastAPI 支持 AI 质量评价 |
| ✅ 修复 `.env` 加载 | `demo/api/_common.py` | Vercel serverless 函数也能读取环境变量 |
| ✅ 修复静态文件挂载 | `server.py` | `dist/` 完整挂载到 `/`，支持 SPA fallback |
| ✅ 构建生产版本 | `demo/dist/` | `npm run build` 生成 |

---

## 🚀 推荐下一步行动

1. **立即处理**: 解决 #0 AI 评价不可用（不修这一项，#9/#10/#11 都是空中楼阁）
2. **本周内**: 修复 #1 前端样式缓存 + 更新 #3 README 启动文档
3. **下个迭代**: 按 #9 固化评价规范（先做 9.1 权重锚点和 9.2 Pydantic 契约，其他逐步推）
4. **再下一轮**: 按 #10 升级评价页 UI（先做 10.1 信息层级，10.3 动效最后做）
5. **方法论迭代**: 按 #11 逐场景引入方法论（先做 11.1 Timeboxing + 11.5 架构，再推其他场景）