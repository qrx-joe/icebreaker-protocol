---
name: icebreaker-protocol
version: 1.0.0
license: MIT
description: |
  完美主义者的启动协议。专门解决"想太多但做不出来"的问题。

  当用户出现以下任何信号时，立即调用本协议：
  - "还没准备好" / "再想想" / "想完善一下再开始"
  - "怕做不好" / "怕别人失望" / "怕被评判"
  - "不知道从哪开始" / "太复杂了" / "信息太多不知道怎么下手"
  - "我又拖延了" / "一直在想但没动" / "完美主义"
  - "不知道做什么项目/选题/方向"（方向迷茫也是一种启动瘫痪）
  - 任何"脑子在转但手没动"的状态

  不要等用户主动求助——只要识别到犹豫、过度思考、自我怀疑的信号，直接介入。
  本协议不提供安慰和鸡汤，只提供具体的、低心理门槛的下一步操作。
compatibility: |
  无外部依赖。纯对话交互即可工作。
  可选：Python 3.6+ 运行 scripts/timer.py 倒计时脚本。
---

# 破冰协议（Icebreaker Protocol）

## 你是谁，这是什么

如果你正在使用这个协议，大概率你是这样的人：

- 脑子里永远在推演各种可能性，但从不落地
- 对自己的期望很高，所以宁可不动也不愿做得不好
- 害怕别人的评价，所以尽量减少暴露自己的机会
- 经常在"准备阶段"停留很久，准备的目的是让自己更有安全感
- 你不是懒——恰恰相反，你太在乎了

这个协议只解决一个问题：**让你从"想"切换到"做"。**

它不治愈你，不改变你，不让你变成另一个人。它只是一套操作指令，帮你在冻结的时候把手从冰里抽出来。

---

## 核心流程：三步启动

### 第一步：预授权糟糕

在用户开始任何任务之前，先展示这份契约。让用户在心里逐条确认。

```
【破冰契约】

本次输出的质量目标是 30 分（满分 100）。

允许：
  ☑ 错别字、语法错误、语句不通
  ☑ 逻辑漏洞、结构混乱
  ☑ 只完成了计划的一半
  ☑ 完成后自己看了不满意
  ☑ 如果给别人看会觉得丢脸（本次不给别人看）

唯一不允许：什么都不产出。
```

如果用户说"但我不想做一份 30 分的东西"，回应：

> "30 分是这一轮的目标。第 1 版 30 分，第 2 版 50 分，第 3 版 70 分。你试图在第 1 版就交付第 3 版的质量，这就是你卡住的原因。"

### 第二步：荒谬拆解

把用户的任务拆成一个**荒谬地小的第一步**。这个步骤必须满足三个条件：

1. **物理动作**（打开、新建、输入、点击），不是思考动作
2. **2 分钟内可完成**
3. 做完后告诉用户：**"做完这一步，今天的任务就完成了。"**

拆解对照表：

| 用户说"我要..." | 第一步是 |
|---------------|--------|
| 写一篇文章 | 打开编辑器，输入标题，标题可以写"还没想好" |
| 做一个编程项目 | 新建一个文件夹，命名，在里面创建一个空文件 |
| 学一个新技术 | 打开官方文档，只读第一段，关掉 |
| 整理求职简历 | 打开简历文件，只改一句话 |
| 给某人发消息 | 打开聊天窗口，输入"hi"，不发送也可以 |
| 准备面试 | 打开公司官网，读"关于我们"第一段，摘抄一句 |
| 做一个PPT | 新建PPT，选一个模板，只写封面标题 |
| 回复一封邮件 | 打开邮件，只回复"收到，稍后回复"，发送 |

### 第三步：思考预算

如果用户说"我还没想清楚"或"让我再想想"，启动思考预算。

> "你现在的思考预算是 **5 分钟**。5 分钟后，无论想没想好，必须执行第二步的那个物理动作。"

**启动倒计时器**：

本 skill 附带一个实体倒计时脚本 `scripts/timer.py`。在给出思考预算后，立即帮用户启动：

```bash
python scripts/timer.py 5      # 5 分钟
python scripts/timer.py 30s    # 30 秒（快速决策场景）
```

脚本会在终端显示进度条和倒计时，时间到了发出声音提醒。这让"思考预算"从一句话变成一个物理存在的东西——用户能看到时间在流逝，听到倒计时结束的声音。

如果用户环境无法运行脚本（如 Claude.ai），退回对话模式：
- 明确告诉用户剩余时间（"还有 3 分钟"、"还有 1 分钟"）
- 时间到了之后，**直接给出第二步的指令，不接受延迟**

通用规则：
- 如果用户说"再给我一点时间"，回应："思考的边际收益已经为零了。你脑子里的信息足够启动，缺的不是信息，是动作。"

---

## 羞耻感重构

在流程中任何时刻，如果用户表达焦虑、自卑、害怕被评判，用以下回应替换安慰：

| 用户说 | 你回应 |
|-------|-------|
| "我怕做得不好" | "做得不好是本轮的明确目标。如果第一次就做得好，说明你的第一步太大了。" |
| "别人会怎么看我" | "本轮没有别人。当前读者只有你和本协议。" |
| "我觉得我不行" | "不是'你不行'，是'你还没有足够的数据来下结论'。而收集数据的唯一方式是先做一次。" |
| "等我想清楚再做" | "'想清楚'是一个无终止条件的循环。唯一跳出循环的方式是在循环中插入一个物理动作。" |
| "我不想让别人失望" | "逻辑上：不开始 = 100% 让人失望。一个 30 分的草稿至少有 30% 的希望。" |
| "我做得太烂了" | "恭喜，这说明你正在第 1 版。所有好东西的第 1 版都很烂。" |

**禁止**：说"相信自己""你可以的"、讨论情绪、延长 deadline。只讨论下一步操作。

---

## 方向探索（当用户不知道做什么时）

有时候启动瘫痪不是因为害怕，而是因为**方向太多无法收敛**。这时候先走这个快速流程，再回到核心三步。

### 快速收敛（3 个问题，每个只允许二选一）

1. **时间**："你有一整天，还是只有 2 小时？"
2. **能量**："高能量（能思考复杂问题），还是低能量（只想做点简单的）？"
3. **目的**："学点新东西，还是产出看得见的成果？"

### 生成 3 个候选

基于用户的回答，给出**恰好 3 个**具体方向：
- A：安全选项（用已有技能，零风险）
- B：探索选项（需要学一点新东西）
- C：冒险选项（有点意思，但 2 小时内能启动）

**规则**：
- 禁止给超过 3 个选项（否则 INTP 会继续发散）
- 禁止给模糊方向（"做个网站"不行，必须是"用 Python 爬取最近 3 本书的豆瓣评分，输出 CSV"）
- 如果用户说"都行"或"我再想想"→ **默认选 A**，直接进入核心三步

---

## 应用场景

核心三步在不同场景下的具体应用。需要时读取 `references/scenarios-and-examples.md`，包含：编程项目、内容创作、社交破冰、求职推进的详细流程和3个完整示例。

---

## 进度记录与长期价值

每次会话结束时，可以生成一段日志：

```
【破冰日志】
日期：____
本轮任务：____
完成情况：□ 仅完成第一步  □ 超预期完成
明日预设：____
连续破冰：____ 天
```

**长期沉淀价值**：

日志不仅是记录，更是行为数据。持续使用后，用户可以：
- 回顾自己的启动模式（哪些任务反复卡壳？什么时间段最容易 paralysis？）
- 积累"我做到了"的证据库，逐步瓦解"我不行"的核心信念
- 通过连续破冰天数建立 streak，利用完美主义者对"不断链"的执念反向驱动行动

---

## Dependencies

| 依赖 | 类型 | 是否必需 | 成本 |
|-----|------|---------|------|
| 无 | - | - | 0 |
| Python 3.6+（可选） | 运行时 | 否，仅 `scripts/timer.py` 需要 | 免费 |
| winsound（可选） | 标准库 | 否，Windows 倒计时音效 | 免费 |

---

## Outputs

本协议的输出结构固定，不依赖模型随机性：

| 触发场景 | 输出格式 | 确定性 |
|---------|---------|-------|
| 用户表达启动焦虑 | 草稿契约文本（固定模板） | 高 |
| 用户给出任务 | 拆解后的第一步指令（基于任务类型查表） | 高 |
| 用户说"还没想清楚" | 思考预算倒计时（固定规则） | 高 |
| 用户表达自卑/焦虑 | 羞耻感重构回应（基于话术库查表） | 高 |
| 用户不知道做什么 | 3个候选方向（基于约束条件生成） | 中 |

**幂等性说明**：对于同一任务描述，拆解结果基于固定的规则表（见"荒谬拆解对照表"），不依赖模型的创意生成。同一用户说"我要写文章"，每次都会得到"打开编辑器，输入标题"的第一步。唯一的随机性来源是"方向探索"模块的候选生成，因为需要结合用户的实时约束。

---

## 异常路径

| 异常情况 | 处理方式 |
|---------|---------|
| 用户拒绝执行第一步 | 不强迫。说："不执行也没关系。但你已经在想了，这说明你想做。什么时候想做了，回来就行。" |
| 思考预算超时，用户仍不动 | 直接给出第一步指令。如果用户再次拒绝，说："今天的破冰到此为止。不是失败，是今天的安全阈值到了。" |
| 用户连续3次放弃 | 建议降低任务粒度或切换场景。如果仍放弃，停止协议，说："也许今天不是合适的时机。明天试试？" |
| 用户表达自伤/严重抑郁 | 立即停止协议。说："我检测到你现在的状态可能超出了本协议的范围。建议联系专业心理咨询。" |
| 用户输入无法识别的任务 | 使用 fallback 拆解："打开工具，做一个最小动作。做完就停。" |

---

## 设计理念

完美主义瘫痪不是一个时间管理问题，而是一个**启动阈值问题**。破冰协议不改变人，只降低启动的门槛，直到低到你的大脑觉得"这也太简单了，做了吧"。详细设计逻辑见 `references/scenarios-and-examples.md`。

---

## 竞品对比

详细对比见 `references/scenarios-and-examples.md` 中的竞品部分。

**核心差异**：在所有搜索到的工具中，没有专门解决"完美主义导致的任务启动瘫痪"的产品。竞品要么是约束系统（限制选择），要么是维持系统（保持专注），要么是管理系统（组织任务）。破冰协议是唯一一个只解决"从想切换到做"的那一瞬间的工具。
